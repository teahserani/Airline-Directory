#include <iostream>

using namespace std; 

extern int *readNumbers(int);
extern void printSubArrays(int *numbers, int length);

int main ()
{
	int n;
	int length;
	printSubArrays(readNumbers(n), length);	
	return 0;
}
