#include <iostream>

using namespace std; 

extern int *readNumbers(int);
extern int *addArrays(int *numbers1,int *numbers2, int length);

int main ()
{
	int n;
	int length;
//	addArrays(readNumbers(n), readNumbers(n),length);	
	return 0;
}

