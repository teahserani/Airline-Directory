#include <iostream>

using namespace std; 

extern int *readNumbers(int);
extern bool sortedArray(int *numbers, int length);

int main ()
{
	int n;
	int length;
	//bool sortedArray(readNumbers(n), length);	
	return 0;
}

