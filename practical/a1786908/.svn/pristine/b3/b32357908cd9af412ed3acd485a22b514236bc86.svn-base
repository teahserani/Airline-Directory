#include <iostream>
#include <stdlib.h>

using namespace std;
extern void cpyda(double*old_array, double *new_array, int);
int main()
{
	int length=3;
	double *old_array= new double [3];
  	for (int i=0; i < 5; i++)
   	{      
       		old_array[i] = i + 1.1;
   	}
        double *new_array= new double [3];
   	cpyda(old_array, new_array,5);
}

