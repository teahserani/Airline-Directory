#include <iostream>
#include <vector>
//#include <algorithm>
#include <stdlib.h>

extern std::vector<int> transform(int*,int);

using namespace std;

int main(int argc, char **argv)
{
	int num=73;
	int base=2;
	cout<<transform(num,base)<<endl;
//	vector<int> base2=transform(num,base);
	return 0;
}
